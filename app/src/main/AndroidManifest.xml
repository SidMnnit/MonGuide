<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.monguide.monguide">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label= "@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.monguide.monguide.loginandsignup.LoginActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.monguide.monguide.loginandsignup.SignUpActivity"
            android:windowSoftInputMode="adjustPan"
            android:launchMode="singleTop">
        </activity>

        <activity android:name="com.monguide.monguide.home.HomeActivity"
            android:launchMode="singleTask">
        </activity>

        <activity android:name="com.monguide.monguide.profile.ViewProfileActivity"
            android:launchMode="singleTop">
        </activity>

        <activity android:name="com.monguide.monguide.question.AddQuestionActivity"
            android:windowSoftInputMode="adjustPan"
            android:launchMode="singleTop">
        </activity>

        <activity android:name="com.monguide.monguide.questionandanswer.FullQuestionActivity"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop">
        </activity>

    </application>
</manifest>
